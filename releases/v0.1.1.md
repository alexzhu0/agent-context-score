# v0.1.1

## What changed

- Added a regression test that locks `.cursor/rules/*.md` discovery to Markdown files only.
- Kept non-Markdown files under `.cursor/rules/` out of scans to avoid noisy or binary inputs.
- Updated package metadata for the patch release.

## Example

```bash
PYTHONPATH=src python3 -m agent_context_score examples/bad --format markdown
```
